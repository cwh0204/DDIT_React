2025-11-13

Node 설치 => NVM(Node Version Manager) => 멀티 버젼 설치
          => Node 설치하면 => JS를 OS위에서 실행 가능
          => JRE이 비교될 수 있음, JS 인터프리터 방식

NPM => Node Package Manager => Maven 빌더에 비유될 수 있음
       필요한 모듈 설치/삭제를 편하게 해줌
       Node를 설치하면 자동으로 같이 설치됨
       Package.json에 설치한 모듈이 dependency로 등록됨
       Package-lock.json 파일은 설치한 모듈의 개별 버젼정보를 가짐
       Package.json 파일의 script에 명령어 등록가능
                start/test만 run 생략가능 npm start/npm run start 생략가능
        모듈 검색은 어디서 npmjs => maven reository
        배포시 node_modules 폴더는 주지 않는다(용량만 크다)
        
webserver 만듬 => express 이용 => Spring으로 하기 때문에 그리 중요하지 않다

원시타입 string number boolean          => 복사
객체타입 원시타입 아닌 거 전부 (function도 객체)      => 참조(별명)

var (생명의 경계선 function키워드) vs let/const ( {} 기호가 생명의 경계선)
const 주의 사항 : 객체 타입인 경우 속성과 메소드 곧 내용물은 바꿀수 있음

Wrapper(랩퍼) 불편한 원시타입을 편하게 쓰게 해주는 것
    string => String, number => Number, boolean => Boolean

자동형변환(알아서 처리, 모르면 짜증날 수 있음)

JSON => 배열식 접근법 정말 중요하다.
함수 => 끝에 return이 생략되어 있다. 왜 return은 함수의 종료를 알린다.
        if else 에서 else 부분 생략이 가능하다
        함수 호출된 자리에는 retrun값이 남는다.
콜백함수 => 함수의 매개변수로 넘어가는 함수 => 당연히 뒤에 불릴 수 밖에 없어서 롤백(callback)
            함수의 선언과 / 호출을 헥살리기 사작한다.(연습시간을 투자해야 한다.)
            개인적으로 여기에 조금 시간 투자 하기를...

전역변수 선언은 window의 메소드 선언과 같고, 전역변수 선언(var)은 window의 속성 선언과 같음.
메소드 안의 this는 해당 메소드를 가진 객체를 가리킴.
참조를 이용해서 부르는 형태를 바꾸면 this가 가리키는 값이 바뀌어버림.

call / apply 사용법이 매개변수 넘기는 법 만 다름. apply로 무조건 배열
bind  사용법이 call 하고 똑같은데, 내부적으로 새함수를 return해줌

=> this를 제어하고 싶을 때는 화살표 함수를 사용하지는 않는다.(제약사항)

function 키워드 => 일반 함수  return; (자바로 치면 void)
                class의미(생성자함수 공장) return this; 앞에 new를 붙여서 사용
                메소드 안의 this => 자신(메소드)를 소유한 객체
                이 this가 가리키는 값을 바꾸고 싶을 때,  call/apply/bind를 사용

prototype => 메소드 공유 (js는 명시적으로 기술해줘야 공유가 이루어짐)

선언적 프로그래밍

배열 -> forEach(자기자신 -> 메소드 체이닝) / map(새 배열 리턴) /filter(새 배열 리턴) / sort(?) 
        리엑트(프론트 프레임워크)에서 아주 많이 사용됨!

화살표함수(arrow function) => 매개변수 1개 ()를 생략 가능
                            구현 블락 => {} 안에 return 문 밖에 없다면 {}랑 return 생략가능
                            call / apply / bind 적용 불가 => this를 제어할 수 없다
                            그럼 this를 제어하고 싶다면 function 키워드를 사용한다.

sort 메소드 완성
        최대 / 최소값 구하기 => 자리바꾸기 => 자리바꾸는 조건을 callback의 return값으로
        객체는 그 자체로 크기 비교가 불가, 조건(원시타입)이 있어야 함,

배열의 메소드 forEach / map / filter / sort

모달? 거의 css, js로 제어한 건 display="none" or display="block"

브라우져 저장공간 => indexedDB / localStorage   ...기술적으로 offline 상태를 잠시 online인 것처럼
                                                사용자를 속일 수도 있음!
                                                의도적으로 사용하면 서버와의 통신 횟수를 줄여서
                                                서버 부하를 많이 줄일 수도 있음..사용하기 나름

localStorage => string밖에 저장이 안되서 JSON.strgitf <=> JSON.parcse
                Cookie 대용으로 많이 사용(용량도 더 크고 사용도 더 쉬움)

게시판 진행, queryString name=value&naem=value
            request.getParameter, request.getParameterValue
            write.html (체크로직은 많이 안해도 됨? 알아는 둬야 함! 회사가면 정말 많이하니까
            상업적인 건 -> 돈 받고 하는건 아주 꼼꼼 해야하니까)

localStorage를 이용해서 CRUD 게시판 작성
                        @post @Get @put @Delete => B/E 맵핑 => Restful(가장 유행하는 방식)

localStorage를 이용한 게시판은 실제 게시판으로 동작하려면 B/E 단에 저장공간을 만들어서
동기화 하지 않으면 안됨 현 상태는 일기장 / 메모장 / todolist등으로 서버에 부하를 주지 않은
프로그램으로 사용가능 (PWA Progressive Web Application 방식)

JQuery 매개변수 없으면 읽기( 첫번째 요소)
       매개변수가 문자열이면 전체쓰기()
       부분조작 콜백함수 => (인덱스,해당 element의 값)

사용시 주의 사항 => $("aaa").html(), $("aaa").val() => 메소드 체이닝이나 변수(권장)로 받아서 쓴다

ES6 이상, 새로운 기능이 많이 들어갔고, 이것들이 프론트 프레임워크에 아주 많이 사용됨
        template String => `` 최대 장점은 멀티라인!
                           java/Spring jsp에 사용시 문제 1개
오직 jsp에서만 const myName = `\${name}` // EL과 template String 문법이 완벽하게 일치함
        자바스크립트 변수라면 앞에 Escape Squence \ 역슬러쉬 처리를 해줘야 함!
        tagged Template => 요건 라이브러리 만드는 경우외에는 거의 안씀! (template)
        일단 존재만 styled-components란 라이브러리가 이걸 이용해서 만들어졌음
Arrow function (화살표 함수) => 이건 넘어가고
전개 연산자 (Spread Operator ...)와 객체 구조 분해 (Object Destruction)은
프론트 프레임워크에서 미친듯이 사용되므로 절대적으로 알아야 함!

템플릿 패턴 => 너무 문자열 더하기만 많이 하지말고, 화면 안에 양식을 숨겨놓고 clonNode(true) => 깊은 복사 => display 보이고 / 안보이고랑 같이 연동하면 Good

전개 연산자     ...(객체 복사 , {} , []) , 구조 분해 => React에서 상습적으로 쓰기 때문에 꼭 눈에 익혀야함
                시간 투자만 조금 하면, 금방 손에 붙음... 예습 / 복습  2중 하나는 항상

fetch => ajax 편하게 쓰는 api
         ajax 모르면 프론트 프레임워크 할 수가 없음(왜? SPA 방식이라서)

event => 등록법 3개 lnline 등록 => on 이벤트명 = 헨들러 => addEventListener(이벤트명, 콜백)
      => 이벤트 버블링 => 자식에게 발생된 이벤트가 부모에도 똑같이 전달됨 => event.stopPropagation
      => 이벤트 캡쳐링 (특히 느낌이 싸 할때)

ajax = > 비동기(일꾼이 최소 2명이어야 가능 하다) => 동시에 일이 진행됨 => 현실은 대부분 비동기
         현실에서 계획을 기반으로 순서를 정한거
         new XMLHttpRequest(); // 퀵 서비스 아저씨, 프로그램 세게에선 필요할 때 마다 부를 수 있음
         // 모든 ajax 라이브러리 핵심은 이거 하나! , 포장만 다름

JSONP => JSON PADDING => CORS 제약사항 없이 사용 가능 => 좋은 기술
원본 ajax => JQuery Ajax => Promise

fetch => 장단점 => built.in => then 두번 써야 결과 가져온다, text(), json()
axios => 위의 모든 단점을 해결해 주었지만 => 외부 라이브러리란게 최대 단점

json-server => 핵심은 간단한 restful server를 제공해 준다.
                restful? get => 읽기, post => 쓰기, put => 수정, delete => 삭제
                boomerang => rest client 프로그램  (주로 B/E 개발자가 많이 쓰게 됨!)
                             postman(제일 유명한 프로그램 => 무료회원가입!)
                

