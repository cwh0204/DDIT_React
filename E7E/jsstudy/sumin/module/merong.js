export const sumin = (insaMsg) => {
    return `${insaMsg} 수민`;
}

export const jiwon = (insaMsg) => {
    return `${insaMsg} 지원`;
}

export const sh = (insaMsg) => {
    return `${insaMsg} 선회`;
}

const total = {sumin,jiwon,sh};
export default total;